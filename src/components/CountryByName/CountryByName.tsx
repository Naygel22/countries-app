import { useParams } from "react-router-dom";


export const CountryByName = () => {
  const params = useParams<{ name: string }>();
  //const {data, isLoading, error} = 


  return (
    <div>CountryByName</div>
  )
}
